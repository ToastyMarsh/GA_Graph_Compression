package subgraph;

import java.util.List;
import graph.Graph;
import linkedgraph.LinkedGraph;

public class PreGARun {
    // Do a pass of the graphs, make a list of all the subgraphs, take their fake links, and then compress them.
    // Graph.fakeLinks()
    // Graph.merge()

    // We will use the Bron-Kerbosch algorithm to find all maximal cliques in the graph.
    // We can then merge all the nodes in each maximal cliques to an arbitary node in that clique

}


